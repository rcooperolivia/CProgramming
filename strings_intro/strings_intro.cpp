#include <iostream>
#include <string>

int main(void){
    // create a string
    std::string str = "Hello";
    
    std::cout << str << std::endl;
    std::cout << "the size of the string is " << str.size() << std::endl;

    // concentrate strings
   // str += " World!";
    //std::cout 


    return 0;
}